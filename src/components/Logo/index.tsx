import { Twitter } from "lucide-react";
import * as S from "./styles";

const Logo = () => {
  return (
    <S.IconContainer>
      <Twitter size={32} />
    </S.IconContainer>
  );
};

export default Logo;
